{
  "name": "SolAI",
  "version": "1.0.0",
  "description": "Unfiltered AI Chatbot with MongoDB & LLM",
  "private": true,
  "scripts": {
    "start": "node backend/server.js",
    "dev": "concurrently \"cd backend && nodemon server.js\" \"cd frontend && npm start\"",
    "build": "cd frontend && npm run build",
    "lint": "eslint ."
  },
  "dependencies": {
    "concurrently": "^7.6.0",
    "dotenv": "^16.3.1"
  }
}
